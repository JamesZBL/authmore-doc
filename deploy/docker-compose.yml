version: "3"

services:
  doc:
    image: registry-vpc.cn-beijing.aliyuncs.com/letec/authmore-doc
    build:
      context: ..
      dockerfile: Dockerfile
    restart: unless-stopped
    networks:
      - authmore-doc
    ports:
      - "3001:80"

networks:
  authmore-doc:
